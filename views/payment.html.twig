<!DOCTYPE html>
<html>
<head>
    <title>Total Fees Calculation</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="text-center mt-5 mb-5">Total Fees Calculation</h1>

        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th>Course Name</th>
                    <th>Credits</th>
                    <th>Fee per Credit</th>
                    <th>Total Fee</th>
                </tr>
            </thead>
            <tbody>
                {% set totalFees = 0 %}
                {% for course in payment %}
                <tr>
                    <td>{{ course.name }}</td>
                    <td>{{ course.credits }}</td>
                    <td>975</td> 
                    <td>{{ course.credits * 975 }}</td> 
                </tr>
                {% set totalFees = totalFees + (course.credits * 975) %}
                {% endfor %}
            </tbody>
        </table>

        <h3>Total Amount: {{ totalFees }}</h3>
    </div>

</body>
</html>
